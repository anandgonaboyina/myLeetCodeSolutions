*/
//without set
/*
public:
    int removeDuplicates(vector<int>& nums)
    {
        int n=nums.size(), k=0;

class Solution {
        if(n<2) return n;
};
        }
        return st.size();
    }
        for(int i=0; i<n; i++)
        {
            st.insert(nums[i]);
        int n = nums.size();
        set<int> st;
public:
    int removeDuplicates(vector<int>& nums) 
    {
/*
class Solution {