            n1= findIntEqualant(s[i]);
            if(i < s.size()-1)
            {
        for(int i=0; i<s.size(); i++)
        {
        /*
        int ans=0;
        int n1=0, n2=0;
    }
    int romanToInt(string s) {
       }
       return 0;
            return 500; break;
        case 'M':
            return 1000; break;
            return 100; break;
        case 'D':
        case 'L':
            return 50; break;
        case 'C':