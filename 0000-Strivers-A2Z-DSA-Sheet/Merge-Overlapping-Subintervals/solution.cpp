        }
        else
            {
                arr[i+1][1]=arr[i][1];
            }
        {
            arr[i+1][0]=arr[i][0];
            if(arr[i][1]>arr[i+1][1])
        {
        if(i<n-1 && arr[i][1]>=arr[i+1][0])
        sort(arr.begin(), arr.end());
        vector<vector<int>> ans;
        for(int i=0;i<n; i++)
    {
        int n= arr.size();
class Solution {
public:
    vector<vector<int>> mergeOverlap(vector<vector<int>>& arr) 
//my optimal brute force solution
/*