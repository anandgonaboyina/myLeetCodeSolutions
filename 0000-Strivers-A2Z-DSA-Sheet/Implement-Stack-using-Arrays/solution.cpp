        stackArray[++topIndex] = x;
    }
            cout<<"stack overflow"<<endl;
            return;
        }
        if(top >= capacity - 1;)
        {
    
    void push(int x) {
        stackArray = new int[capacity];
        topIndex = -1;
    }
    ArrayStack(int size = 1000) {
        capacity = size;
int capacity;
int topIndex;
public:
privte:
int * stackArray;
class ArrayStack {